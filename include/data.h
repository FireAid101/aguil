#ifndef DATA_H
#define DATA_H

#include <stdlib.h>
#include <stdio.h>
#include <SDL2/SDL.h>

typedef SDL_Color color;

typedef struct
{
	int x, y;
} vec2; // A simple data structure for storing 2d points and values

#endif
